apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: client-secret
  namespace: {{ NAMESPACE }}
  labels:
    app: client
spec:
  itemPath: "vaults/{{ ONEPASSWORD_OPERATOR_VAULT }}/items/client"
