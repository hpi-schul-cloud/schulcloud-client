{{#extend "lib/simple"}}
  {{#content "scripts" mode="append"}}
      <script src="/scripts/dataprivacy/dataprivacy.js" type="text/javascript" defer></script>
      <script src="/scripts/dataprivacy/registration.js" type="text/javascript" defer></script>
      <script src="/scripts/jquery/jquery.datetimepicker.full.min.js" type="text/javascript" defer></script>
  {{/content}}
  {{#content "styles" mode="append"}}
      <link rel="stylesheet" href="/styles/administration/dataprivacy.css"/>
      <link rel="stylesheet" href="/styles/lib/datetimepicker/jquery.datetimepicker.min.css"/>
  {{/content}}
  {{#content "page"}}
    {{#embed "lib/forms/paginatedForm" sections=5 action="/registration/submit" method="post" class="registration-form parent"}}
      {{#content "panels"}}
      <section data-panel="section-1">
        <h4>Willkommen zur Anmeldung in der {{theme.title}}</h4>
        <p>Wir freuen uns, dass du die {{theme.title}} nutzen möchtest und mithilfst, gemeinsam den nächsten Schritt zur digitalen Unterstützung des Unterrichts zu gehen – Open Source, nicht profitorientiert und datenschutzkonform.</p>
        <p>Bitte registriere dich zunächst in der {{theme.title}} und stimme den Nutzungsbedingungen zu, damit du die Vorteile der digitalen Lernumgebung nutzen kannst.</p>

        <p class="mt-2 explanation nomargin">* Sprachlicher Hinweis: Für bessere Lesbarkeit verwenden wir in der {{theme.short_title}} die männliche Form (z.B. „Schüler“, „Lehrer“). Es sind stets alle Personen unabhängig vom Geschlecht gemeint. Die {{theme.short_title}} ist für alle Menschen da, unabhängig von Herkunft, Einschränkungen, Geschlecht und sexueller Orientierung. 🌈🎉🦄 </p>
        
        <input class="form-control linked" name="importHash" type="hidden" value="{{user.importHash}}"/>
        {{#if classOrSchoolId}}<input type="hidden" name="classOrSchoolId" value="{{classOrSchoolId}}" />{{/if}}
        {{#if user.userId}}<input type="hidden" name="userId" value="{{query.userId}}" />{{/if}}
      </section>



      <section data-panel="section-2">
        <h4>Ihre Daten</h4>
        <p>Die {{theme.short_title}} bewahrt Nutzerdaten sicher auf und gibt sie nicht an Dritte weiter. Die Verarbeitung der Daten erfolgt entsprechend der hohen gesetzlichen Datenschutz-Anforderungen.</p>

        <p>Für die Nutzung der {{theme.short_title}} benötigen wir folgende Angaben:</p>
        <div class="row">
          <div class="form-group col-md-6 col-sm-12">
            <label>Vorname: *</label>
              {{#if user.firstName}}
                  <input class="form-control linked" name="firstname" type="text" value="{{user.firstName}}" readonly required/>
              {{else}}
                  <input class="form-control linked" name="firstName" type="text" placeholder="Ihren Vornamen hier eingeben" required/>
              {{/if}}
          </div>
          <div class="form-group col-md-6 col-sm-12">
            <label>Nachname: *</label>
              {{#if user.lastName}}
                  <input class="form-control linked" name="lastName" type="text" value="{{user.lastName}}" readonly required/>
              {{else}}
                  <input class="form-control linked" name="lastName" type="text" placeholder="Ihren Nachname hier eingeben" required/>
              {{/if}}
          </div>
        </div>

        <div class="form-group">
          <label>Ihre E-Mail-Adresse *</label>
            {{#if user.email}}
                <input class="form-control linked" type="email" name="email" value="{{user.email}}" required/>
            {{else}}
                <input class="form-control linked" type="email" name="email" required placeholder="lehrer@schul-cloud.org" autocomplete />
            {{/if}}
        </div>

          <p style="margin-top:1rem;">Mindestens 8 Zeichen mit Groß- und Kleinschreibung und mindestens einer Zahl und Sonderzeichen.
              <br/>
              Erlaubte Sonderzeichen sind: <code>!§$%&/()=?\;:,.#+*~</code></p>
          <div class="row">
              <div class="form-group col-md-6 col-sm-12" style="margin-top:0;">
                  <label>Passwort: *</label>
                  <input
                          class="form-control linked"
                          id="password"
                          name="password-1"
                          type="password"
                          placeholder="Passwort"
                          minlength="8"
                          pattern="^(?=.*[A-Z])(?=.*[0-9])(?=.*[a-z])(?=.*[\-_!<>§$%&\/()=?\\;:,.#+*~']).{8,255}$"
                          title="Mindestens 8 Zeichen mit Groß- und Kleinschreibung und mindestens einer Zahl und Sonderzeichen."
                          required/>
              </div>
              <div class="form-group col-md-6 col-sm-12" style="margin-top:0;">
                  <label>Passwort wiederholen: *</label>
                  <input
                          class="form-control"
                          id="password-repeat"
                          name="password-2"
                          type="password"
                          placeholder="Passwort wiederholen"
                          minlength="8"
                          pattern="^(?=.*[A-Z])(?=.*[0-9])(?=.*[a-z])(?=.*[\-_!<>§$%&\/()=?\\;:,.#+*~']).{8,255}$"
                          title="Mindestens 8 Zeichen mit Groß- und Kleinschreibung und mindestens einer Zahl und Sonderzeichen."
                          required/>
              </div>
          </div>
      </section>



      <section data-panel="section-3">
        <h4>Einwilligungserklärung</h4>
        <p>Bitte bestätige folgende Einwilligungserklärungen, damit du die Vorteile der {{theme.title}} nutzen kannst.</p>
        <p>Ich, <b data-from="firstName"></b> <b data-from="lastName"></b>, erkläre folgendes:</p>
          {{#embed "registration/forms/einwilligungserklaerung"}}{{/embed}}
      </section>



      <section data-panel="section-4" class="submit-page" data-feature="pin">
        <h4>Fast geschafft!</h4>
        <p>Wir haben soeben eine E-Mail an Ihre angegebene Adresse gesendet. Bitte geben Sie den darin enthaltenen Code hier ein, um die Registrierung abzuschließen. Sie haben keine E-Mail erhalten? <button type="button" class="btn-link" id="resend-pin">Code erneut senden</button></p>
        <div id="pinverification">
            <div id="temp-pin" class="hidden"></div>
            {{#embed "registration/pin" digits=4 pattern="[0-9]" required="true" name="email-pin" class="mail-validation my-1"}}{{/embed}}
        </div>
        <input type="hidden" name="pin-sent" value="no" />
      </section>



      <section data-panel="section-5">
        <h4>Vielen Dank für die Registrierung in der {{theme.short_title}}</h4>
        <div>
          <p>
            Mit folgenden Daten kannst du dich in der {{theme.short_title}} anmelden. Bitte bewahre sie gut auf.
            <div class="print">
              <div class="recap">
                <p>E-Mail: <i data-from="email"></i></p>
                <p>Passwort: <i data-from="password-1"></i></p>
              </div>
              <div class="btn btn-secondary btn-print" data-toggle="tooltip" title="" data-original-title="Anmeldedaten drucken">
                <i class="fa fa-print"></i> Drucken
              </div>
            </div>
          </p>
        </div>
        <p>Du kannst dich jetzt in der {{theme.title}} anmelden.</p>

        <a class="btn btn-primary" href="/login">Login öffnen</a>
      </section>
      {{/content}}
    {{/embed}}
  {{/content}}
{{/extend}}