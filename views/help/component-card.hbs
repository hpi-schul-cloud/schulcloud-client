<div class="help-card {{class}}" id="{{id}}">
    <div class="icon-wrapper">
        <i class="fa {{icon}}" aria-hidden="true"></i>
    </div>
    <div class="card-content">
        <h4 class="card-title">{{title}}</h4>
        <div class="card-body">
            {{#block "card-body"}}{{/block}}
        </div>
    </div>
</div>