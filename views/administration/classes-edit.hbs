{{#extend "lib/loggedin"}}

    {{#content "scripts" mode="append"}}
        <script src="{{ furl('/scripts/administration/classes/edit.js') }}" type="text/javascript" defer></script>
    {{/content}}

    {{#content "styles" mode="append"}}
        <link rel="stylesheet" href="{{ furl('/styles/classes/classes.css') }}"/>
        <!-- dataprivacy is only used for .recap -->
        <link rel="stylesheet" href="{{ furl('/styles/administration/dataprivacy.css') }}"/>
    {{/content}}

    {{#content "page"}}
        <section class="section-classes-create">
            <form class="create-form" method="post" action="">
                {{> "administration/forms/form-classes-edit"}}
            </form>
        </section>
    {{/content}}
{{/extend}}
