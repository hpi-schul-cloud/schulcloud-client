<input name="schoolId" type="hidden" data-force-value="true" value="{{currentSchool}}" />
<input name="roles[]" type="hidden" data-force-value="true" value="{{roles}}" />

<p>
    Importiere Nutzerdaten per .csv-Datei
    (<a href="#" id="csv-import-example">Beispiel</a>)
    und lass deinen Nutzern einen Registrierungslink zukommen.
</p>
<p>
    Mehr Informationen zu Struktur der .csv-Datei, Klassenzuordnung und Ablauf des Registrierungsprozesses findest du
    <a href="https://docs.schul-cloud.org/x/NYBgAw" target="_blank">hier</a>.
</p>
<p>
    <img src="/images/csv_example.png" />
</p>
<div class="form-group">
    <label>CSV-Datei auswählen</label>
    <input class="form-control" name="csvFile" type="file" required />
</div>
<div class="form-group">
    <label>Schuljahr</label>
    <select class="form-control" name="schoolYear" data-placeholder="Schuljahr auswählen" data-force-value="true">
        {{#each years}}
            <option {{#ifeq this._id ../schoolCurrentYear}}selected="selected"{{/ifeq}} value="{{this._id}}">{{this.name}}</option>
        {{/each}}
    </select>
</div>
<div class="form-group">
    <label>
        <input name="sendRegistration" type="checkbox" value="sendRegistration" />
        Registrierungslinks sofort nach Import an Nutzer senden
    </label>
    <p>
        Alternativ kannst du die Registrierungslinks später abrufen und deinen Nutzern per Mail oder QR-Druckbogen zukommen lassen.
    </p>
</div>
