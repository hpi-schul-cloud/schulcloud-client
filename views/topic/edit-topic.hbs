{{#extend "lib/loggedin"}}

    {{#content "scripts" mode="append"}}
        <script src="/vendor/react/react.js"></script>
        <script src="/vendor/react/react-dom.js"></script>
        <script src="/vendor/react/react-sortable-hoc.js"></script>
        <script src="/vendor/ckeditor/ckeditor.js"></script>
        <script src="/scripts/topic.js"></script>
    {{/content}}

    {{#content "styles" mode="append"}}
        <link rel="stylesheet" href="/styles/courses/course.css"/>
    {{/content}}

    {{#content "page"}}
        <section class="section-course-edit">
            <form action="{{action}}" method="post">
                {{#if method}}
                    <input type="hidden" name="_method" data-force-value="true" value="{{method}}" />
                {{/if}}

                <input name="courseId" type="hidden" data-force-value="true" value="{{courseId}}" />

                <div class="form-group">
                    <label>Titel des Themas</label>
                    <input class="form-control" name="name" type="text" placeholder="z.B. Rechnen mit Primzahlen" value="{{lesson.name}}" required />
                </div>

                <div class="form-group">
                    <label>Inhalte</label>

                    <!-- React Magic -->
                    <div id="content-blocks" data-value="{{lesson.contents}}"></div>
                </div>

                <div class="form-group">
                    <input type="submit" class="btn btn-primary" value="{{submitLabel}}" />
                </div>

            </form>
        </section>
    {{/content}}
{{/extend}}