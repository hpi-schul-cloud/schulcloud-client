{{#extend "lib/loggedin"}}

    {{#content "scripts" mode="append"}}
        <script src="/vendor/react/react.min.js" defer></script>
        <script src="/vendor/react/react-dom.min.js" defer></script>
        <script src="/vendor/react/react-sortable-hoc.js" defer></script>
        <script src="/vendor/ckeditor/ckeditor.js" defer></script>
        <script src="/scripts/topicEdit.js" defer></script>
        <script src="/scripts/courses.js" defer></script>
    {{/content}}

    {{#content "styles" mode="append"}}
        <link rel="stylesheet" href="/styles/courses/course.css"/>
    {{/content}}

    {{#content "page"}}
        <section class="section-course-edit">
            <form action="{{action}}" method="post">
                {{#if method}}
                    <input type="hidden" name="_method" data-force-value="true" value="{{method}}" />
                {{/if}}

                <input name="courseId" type="hidden" data-force-value="true" value="{{courseId}}" />
                
                <input name="position" type="hidden" data-force-value="true" value="{{lesson.position}}" />

                <div class="form-group">
                    <label>Titel des Themas</label>
                    <input class="form-control" name="name" type="text" placeholder="z.B. Rechnen mit Primzahlen" value="{{lesson.name}}" required />
                </div>

                <div class="form-group">
                    <label>Inhalte</label>

                    <!-- React Magic -->
                    <div id="content-blocks" data-value="{{lesson.contents}}" data-etherpadbaseurl="{{etherpadBaseUrl}}"></div>
                </div>

                <div class="form-group">
                    <input type="submit" class="btn btn-primary" value="{{submitLabel}}" />
                </div>

            </form>
        </section>
    {{/content}}
{{/extend}}
