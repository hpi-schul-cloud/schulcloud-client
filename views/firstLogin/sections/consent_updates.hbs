<section data-panel="section-{{section-nr}}" {{#if submit-page}}class="submit-page" {{/if}}>

  <h4>Aktualisierung deiner Einwilligungserklärung{{#ifCond @root.updatedConsents.total '>' 1}}en{{/ifCond}}</h4>
  <p style="margin-bottom: 20px;">Für ein noch besseres Lernerlebnis wird die {{@root.theme.title}} stetig
    weiterentwickelt. Im Zuge dessen hat sich
    {{#ifCond @root.updatedConsents.termsOfUse.total '>' 0}}die Nutzungsordnung{{/ifCond}}
    {{#ifCond @root.updatedConsents.privacy.total '>' 0}}
    {{#ifCond @root.updatedConsents.termsOfUse.total '>' 0}}
    und
    {{/ifCond}}
    die Datenschutzerklärung
    {{/ifCond}}
    erweitert.
  </p>
  <div id="consentUpdateFields">
    {{#if @root.updatedConsents.haveBeenUpdated}}

    {{#each @root.updatedConsents.all.data}}
    <h5>Zusammenfassung der Änderungen in der <a href="{{this.url}}" target="_blank">{{this.visualType}}</a> zum
      {{dateToPicker this.publishedAt}}</h5>
    <div class='privacyStatement card'>
      <div class="card-header">
        <h5 style="margin:0;">
          {{this.title}}
        </h5>
      </div>
      <div class="content card-block">
        {{{this.consentHTML}}}
      </div>
    </div>
    {{/each}}

    <div>
      {{#if @root.updatedConsents.privacy.total}}
      <label>
        <input name="privacyConsentVersion" type="hidden" value="true" />
      </label>
      {{/if}}
      {{#if @root.updatedConsents.termsOfUse.total}}
      <label>
        <input name="termsOfUseConsentVersion" type="hidden" value="true" />
      </label>
      {{/if}}
    </div>
  </div>
  {{/if}}

</section>
