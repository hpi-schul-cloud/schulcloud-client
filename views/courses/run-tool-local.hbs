{{#content "scripts" mode="append"}}
    <script defer>
        setHeight = function() {
            // dirty fix for digitalgipfel
            $('.lti-frame').css({
                'height': ($(document).height() - $('.navbar').height() * 3) + 'px'
            });
        };
    </script>
{{/content}}
{{#extend "lib/loggedin"}}

    {{#content "styles" mode="append"}}
        <link rel="stylesheet" href="/styles/tools/tools.css"/>
    {{/content}}

    {{#content "page"}}
        <iframe class="lti-frame" src="{{tool.url}}" frameborder="0" onload="setHeight()"></iframe>
        <a href="{{tool.url}}" target="_blank" class="pull-right">in neuem Tab öffnen</a>
    {{/content}}
{{/extend}}