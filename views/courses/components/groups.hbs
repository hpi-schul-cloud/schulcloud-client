<div class="row">
  <div class="col-sm-12">
    {{#ifneq 0 (arrayLength courseGroups)}}
      <div class="course-groups-header">
        {{#userHasPermission "COURSE_EDIT" }}
        Schülergruppen
        {{else}}
        Meine Schülergruppen
        {{/userHasPermission}}
      </div>
      <div id="courseGroups">
        <ol class="courseGroup">
          <div class="row">
            {{#each ../courseGroups}}
            <div class="col-sm-6 col-xs-12">
              {{> 'courses/components/courseGroup-card'}}
            </div>
            {{/each}}
          </div>
        </ol>
      </div>
    {{else}}
      <div class="empty-state">
        <img src="/images/empty-states/gruppe.png" alt="Keine erstellte Gruppe" class="image"/>
        <h4>Erstelle Gruppen innerhalb eines Kurses</h4>
        <h6 class="sub-title">
          Mithilfe von Schülergruppen kannst du deine Teilnehmer*innen innerhalb eines Kurses in Teams aufteilen, z.B. für Gruppenarbeiten.
        </h6>
      </div>
    {{/ifneq}}

    {{#userHasPermission "COURSEGROUP_CREATE"}}
    <div class="add-button">
      <a href="/courses/{{../id}}/groups/add" class="btn btn-primary {{#ifneq 0 (arrayLength ../courseGroups)}}w-100{{else}}btn-empty{{/ifneq}}" title="Gruppe hinzufügen">
        <span>Erstelle eine neue Gruppe</span>
      </a>
    </div>
    {{/userHasPermission}}
  </div>
</div>
