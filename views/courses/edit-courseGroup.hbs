{{#extend "lib/loggedin"}}

    {{#content "styles" mode="append"}}
        <link rel="stylesheet" href="{{getVersionedPath '/styles/courses/course.css'}}"/>
        <link rel="stylesheet" href="{{getVersionedPath '/styles/lib/datetimepicker/jquery.datetimepicker.min.css'}}"/>
    {{/content}}

    {{#content "scripts" mode="append"}}
        <script src="{{getVersionedPath '/scripts/jquery/jquery.datetimepicker.full.min.js'}}" type="text/javascript" nonce="{{nonceValue}}" defer></script>
        <script src="{{getVersionedPath '/scripts/moment/moment.js'}}" type="text/javascript" nonce="{{nonceValue}}" defer></script>
        <script src="{{getVersionedPath '/scripts/courses.js'}}" type="text/javascript" nonce="{{nonceValue}}" defer></script>
        <script src="{{getVersionedPath '/scripts/coursesTimes.js'}}" type="text/javascript" nonce="{{nonceValue}}" defer></script>
    {{/content}}


    {{#content "page"}}
        <section class="section-course">
            <form action="{{action}}" method="post">
                {{#if method}}
                    <input type="hidden" name="_method" data-force-value="true" value="{{method}}"/>
                {{/if}}

                <input name="schoolId" type="hidden" data-force-value="true" value="{{currentSchool}}"/>
                <input name="courseId" type="hidden" data-force-value="true" value="{{courseId}}"/>

                <div class="form-group">
                    <label for="nameOfGroup">{{$t "courses._course.groups._group.edit.label.nameOfGroup"}}</label>
                    <input id="nameOfGroup" class="form-control" name="name" type="text" placeholder="{{$t "courses._course.groups._group.edit.input.group1"}}" value="{{{courseGroup.name}}}" required/>
                </div>

                <div class="form-group">
                    <label for="studentParticipant">{{$t "courses._course.global.label.studentParticipant"}}</label>

                    <select id="studentParticipant" name="userIds[]" multiple data-placeholder="{{$t "courses.global.input.selectStudents"}}">
                        {{#each students}}
                            <option value="{{this._id}}" {{#if this.selected}}selected{{/if}}>
                                {{#if this.displayName}}
                                    {{this.displayName}}
                                {{else}}
                                    {{this.firstName}} {{this.lastName}}
                                {{/if}}
                            </option>
                        {{/each}}
                    </select>
                </div>


                <div class="modal-footer">
                    <button class="btn btn-add btn-secondary btn-cancel" type="button">
                        {{closeLabel}}
                    </button>

                    {{> "lib/components/csrfInput"}}

                    <button type="submit" class="btn btn-primary btn-submit">
                        {{submitLabel}}
                    </button>
                </div>

            </form>

            {{#embed "lib/components/modal-cancel" class="cancel-modal"}}

            {{/embed}}
        </section>
    {{/content}}
{{/extend}}
